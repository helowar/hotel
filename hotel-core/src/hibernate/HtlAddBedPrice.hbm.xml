<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE hibernate-mapping PUBLIC "-//Hibernate/Hibernate Mapping DTD 3.0//EN"
"http://hibernate.sourceforge.net/hibernate-mapping-3.0.dtd">
<!-- 
    Mapping file autogenerated by MyEclipse - Hibernate Tools
-->
<hibernate-mapping package="com.mangocity.hotel.base.persistence">
	<class name="HtlAddBedPrice" table="htl_add_bed_price" lazy="false" dynamic-insert="true" dynamic-update="true">
		
		<id name="ID" unsaved-value="0" column="add_bed_price_id">
			<generator class="sequence">
				<param name="sequence">seq_hotel_add_bed_price</param>
			</generator>
		</id>
		<property name="contractId" column="contract_id"/>
		
        <property name="beginDate" column  ="BEGIN_DATE"  >
        </property>
        <property name="endDate" column  ="END_DATE"  >
        </property>
        <property name="roomType" column  ="ROOM_TYPE"  >
        </property>
        <property name="addPrice" column  ="ADD_PRICE"  >
        </property>
        <property name="addPriceType" column  ="ADD_PRICE_TYPE"  >
        </property>
        <!-- 
        <property name="payMethod" column  ="pay_method"  >
        </property>
         -->
        <property name="remark" column="REMARK" />		
		<property name="createBy" column="Create_by" />		
		<property name="modifyBy" column="modify_by" />
		<property name="createById" column="Create_by_id" />		
		<property name="modifyById" column="modify_by_id" />		
		<property name="createTime" column="create_time" />
		<property name="modifyTime" column="modify_time" />
		<property name="active" column="active" />
		<property name="payMethod" column="pay_method" />        
    </class>
    <query name="queryAddBedPrices">
		<![CDATA[ 
		from HtlAddBedPrice addBedPrice where addBedPrice.contractId =? 
		]]>
	</query>
	
	<query name="queryAddBedPricesOrder">
		<![CDATA[ 
		from HtlAddBedPrice addBedPrice where addBedPrice.contractId =? and addBedPrice.roomType = ? and nvl(addBedPrice.payMethod,'pay') = ? order by addBedPrice.modifyTime
		]]>
	</query>
	
	<query name="querySubAddBedPrices">
		<![CDATA[ 
		from HtlAddBedPrice addBedPrice where addBedPrice.contractId =? and addBedPrice.roomType = ? and addBedPrice.modifyTime<=? and nvl(addBedPrice.payMethod,'pay') = ? order by addBedPrice.beginDate
		]]>
	</query>
	
	<query name="queryAddBedPriceForWeb">
		<![CDATA[
		from HtlAddBedPrice addBedPrice where addBedPrice.contractId=? and addBedPrice.roomType=? and not ( addBedPrice.endDate<? or addBedPrice.beginDate>=? ) and nvl(addBedPrice.payMethod,'pay') = ? order by addBedPrice.beginDate
    	]]>
	</query>
	
	<!-- hotel 2.9.2 RMS2962 查询订单的入住日期在某合同下某房型加床的起始日期内的的加床信息 add by chenjiajie 2009-07-23 -->
    <query name="queryAddBedPricesCheckDate">
		<![CDATA[ 
		from HtlAddBedPrice where contractId =? and roomType = ?
		and beginDate <= trunc(?) and endDate >= trunc(?)
		and addPrice > 0
		and nvl(payMethod,'pay') = ?
		]]>
	</query>
</hibernate-mapping>
